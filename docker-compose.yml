# Note: this is for development only. See the example in readme.md for homelab use.

services:
  yarr-dev:
    build: .
    ports:
      - 7070:7070
    volumes:
      - yarr_data:/data

volumes:
  yarr_data: